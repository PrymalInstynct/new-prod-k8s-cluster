---
apiVersion: v1
kind: ConfigMap
metadata:
  name: harimari-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: harimari
        group: external
        url: https://harimari.prymal.info/web/index.html
        interval: 1m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 200"
        alerts:
          - type: discord
